- @page.parts.each do |part|
  .content
    = part.body.html_safe
%ul.large-block-grid-4
  - @page.images.each do |image|
    %li= link_to image_tag(image.thumbnail("300x300#c").url, :class => "imgshadow"), image.url

